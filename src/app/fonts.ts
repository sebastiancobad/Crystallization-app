// Google Fonts are fetched at build time by next/font.
// If the build environment cannot reach fonts.googleapis.com,
// the CSS-variable fallback in globals.css ensures the correct
// system font stack is still applied.

export const fontVariableClasses = "--font-inter --font-jetbrains-mono";
